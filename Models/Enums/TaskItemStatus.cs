namespace Homera.Models.Enums
{
    public enum TaskItemStatus
    {
        Pending = 0,     // Чакаща
        Assigned = 1,    // Назначена
        InReview = 2,    // За преглед
        Completed = 3,   // Изпълнена
        Cancelled = 4    // Отказана
    }
}
